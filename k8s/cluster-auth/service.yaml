apiVersion: v1
kind: Service
metadata:
  name: kube-federated-auth
  namespace: db-ops
spec:
  type: NodePort
  selector:
    app: kube-federated-auth
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
